spring:
  profiles: 
    active: dev
  application:
    name: slcfservice
  cloud:
    config:
      uri: ${SPRING_CONFIG_URI:http://localhost:8888}
      username: user
      password: secret
      failFast: false